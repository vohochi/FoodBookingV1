/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'foodbookingapi.onrender.com',
      'drive.google.com',
    ],
    unoptimized: true
  },
};

export default nextConfig;
